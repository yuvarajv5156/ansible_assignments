#SPDX-License-Identifier: MIT-0
---
# handlers file for roles/tomcat
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart tomcat
  ansible.builtin.systemd:
    name: tomcat
    state: restarted
